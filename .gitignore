# Dependencies — installed by `npm ci` in the build workflow
node_modules/

# Build output — generated by `vite build` in the build workflow
dist/

# Environment variables — never commit secrets
.env
.env.local

# Credential seed — contains password hashes, never commit
supabase/seed_credentials.sql
.verify-artifacts/

# Local
.DS_store